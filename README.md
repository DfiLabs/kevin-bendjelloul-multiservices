# KB MultiServices (vitrine)

Vitrine statique pour **KB MultiServices** (multi-services bâtiment).

## Modifier le contenu

- `content.json` contient le nom, la zone, les services, les photos, les avis, etc.
- Le fichier `main.js` charge `content.json` au chargement de la page.

## Prévisualiser en local

Sur macOS, depuis le dossier `kevin_reno_site`:

```bash
python3 -m http.server 5173
```

Puis ouvrir `http://localhost:5173`.

## Déploiement GitHub Pages

Si vous voulez que je le publie sur GitHub Pages (comme l’autre site), dites-moi sur quel dépôt on le met.

